# Codex 지침

## 프로젝트 목표

소상공인과 예비창업자를 위한 법률정보 도우미 POC를 만든다. 사용자가 법률 질문을 잘 쓰지 못해도 카드형 입력, 추가 질문, 공식 출처 검색, 쉬운 체크리스트를 통해 관련 법률정보를 찾도록 돕는다.

## 작업 언어와 설명 방식

- 기본 언어는 한국어다.
- Windows PowerShell 기준으로 안내한다.
- 비프로그래머도 이해할 수 있도록 파일 위치, 명령어 목적, 실행 후 기대 결과를 함께 설명한다.

## 보안

- API 키, 토큰, 개인정보, 고객정보, 계약서 원문, 내부 URL은 저장소에 저장하지 않는다.
- 실제 키는 `.env.local`에만 둔다.
- `.env.example`에는 변수명만 둔다.
- 계약서 점검 기능을 만들 때도 원문 저장 기능은 기본값으로 만들지 않는다.

## 법률정보 범위

- 이 프로젝트는 법률정보 탐색 보조 도구다.
- 법률 자문, 승소 가능성, 소송 전략, 계약 해지 지시를 제공하지 않는다.
- 답변에는 가능한 한 공식 출처 후보를 포함한다.
- 출처가 불명확한 내용은 단정하지 않는다.

## Codex 검색 기준

새 세션을 시작하면 먼저 아래 파일을 확인한다.

1. `WORKLOG.md`
2. `docs/AI_SEARCH_INDEX.md`
3. `docs/architecture.md`
4. `src/lib/gemini/agents.ts`
5. `src/lib/law/searchLaw.ts`

## 구현 원칙

- 기존 파일을 읽고 구조를 파악한 뒤 수정한다.
- 작은 단위로 구현하고 검증한다.
- 완료 전에는 `git status`, `npm run typecheck` 또는 해당 작업에 맞는 검증 명령을 실행한다.
- 사용자 변경사항을 임의로 되돌리지 않는다.
